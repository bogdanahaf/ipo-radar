ALPHAVANTAGE_API_KEY=replace_me
TELEGRAM_BOT_TOKEN=123456:replace_me
TELEGRAM_CHAT_ID=@your_channel_username
IPO_RADAR_SITE_URL=https://your-github-user.github.io/ipo-radar/

# Optional comma-separated company keywords that should always be treated as interesting.
IPO_WATCHLIST=stripe,databricks,figma,canva,chime,klarna,discord,shein,bytedance,anthropic,perplexity,plaid,brex,navan,rippling,airtable,notion,stubhub

# Optional: adds a short "AI skim" paragraph to the Sunday week Telegram digest only.
# OPENAI_API_KEY=(paste from OpenAI dashboard; never commit the real value)
# OPENAI_MODEL=gpt-4o-mini

# Optional: milliseconds between Alpha Vantage TIME_SERIES_DAILY calls when recapping
# prior digest tickers (free tier ~5 calls/min; default 13000).
# IPO_AV_TIME_SERIES_DELAY_MS=13000
